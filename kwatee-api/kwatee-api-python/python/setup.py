from setuptools import setup

setup(name='kwatee',
    version = '${project.version}',
    packages = ['kwatee', ],
    install_requires = 'poster>=0.8.1',
    author = 'mac',
    license = 'LGPL',
    url = 'http://kwatee.net',
)
